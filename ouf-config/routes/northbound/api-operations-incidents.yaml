apiVersion: ouf.trieste.it/v1
kind: RouteBinding
metadata: {id: api-operations-incidents, version: 1.0.0, status: ACTIVE}
spec:
  direction: northbound
  exposure: public
  capabilityRef: ouf.operations.incidents@1.0.0
  sourceRef: mcp-server-operations@1.0.0
  match: {method: POST, path: /api/v1/operations/incidents}
  backendBinding: {service: ouf-mcp-server, port: 8080, path: /api/internal/v1/mcp/operations/incidents}
  policy:
    identity: OIDC
    allowedActorTypes: [HUMAN]
    maxRequestBytes: 65536
    timeoutSeconds: 5
