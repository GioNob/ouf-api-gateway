apiVersion: ouf.trieste.it/v1
kind: RouteBinding
metadata: {id: mcp-ingestion-history, version: 1.0.0, status: ACTIVE}
spec:
  direction: northbound
  exposure: internal
  capabilityRef: ouf.ingestion.history@1.0.0
  sourceRef: ingestion-runtime-operations@1.0.0
  match: {method: POST, path: /internal/capabilities/v1/execute/ouf.ingestion.history}
  backendBinding: {service: ouf-ingestion-runtime, port: 8080, path: /api/internal/v1/ingestion/operations/history}
  policy:
    identity: M2M
    allowedServiceIdentities: [installation://iam.workloadClients.mcpServer]
    allowedActorTypes: [HUMAN_USER, MCP_SERVER]
    maxRequestBytes: 65536
    timeoutSeconds: 3
