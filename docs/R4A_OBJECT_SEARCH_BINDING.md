# R4a — urban.object.search, prima tranche

Il contratto candidate collega `POST /internal/capabilities/v1/execute/urban.object.search` a `POST /api/udp/v1/objects/search` sul servizio UDP. Il payload è `{ "type": "<tipo canonico esatto>", "pageSize": 1..100, "cursor": "<opzionale>" }`. UDP riusa il serving con tenant, decisioni per oggetto/proprietà, proiezione minimizzata e cursore opaco. Nessun parametro seleziona un backend o una query arbitraria.

**Stato: candidato installabile, tool MCP ancora `INACTIVE`.** `tools.materialize_object_search` aggiunge una sola rotta POST al profilo di incidenti esistente. Riutilizza OIDC workload, delega umana firmata e scope `urban.object.search`; verifica envelope e argomenti, cancella le credenziali in ingresso e invia a UDP solo `{type,pageSize?,cursor?}` insieme a un receipt HMAC di 30 secondi legato ai byte esatti. UDP verifica receipt, tenant e scope, installa il `TrustedPrincipal` e richiede la policy locale prima del serving. La configurazione mancante risponde 503; firma, body o policy non validi rispondono 403.

La CI Gateway esegue `OUF_APISIX_LIVE_TEST=1 python3 -m pytest -q tests/test_execute_apisix_live.py` con APISIX Docker reale, issuer OIDC e owner HTTP di test. Controlla route `/internal/capabilities/v1/execute/urban.object.search`, delega e scope, corpo inoltrato, credenziali rimosse e firma/body hash del receipt; copre anche tenant errato e argomenti vietati. Il job `object-search-pairwise` genera inoltre il receipt con il Lua Gateway e lo fa consumare, con il corpo originale e alterato, dal filtro Java UDP al commit fissato nel workflow; verificati principal, correlation e diniego della policy owner. Il job `object-search-network` usa gli stessi Gateway/APISIX e il servizio UDP Java reale con PostgreSQL e bundle di test: verifica ricerca ammessa, due pagine con cursore opaco, esclusione per tenant/oggetto e `partial` dopo il rifiuto owner. La CI UDP verifica separatamente la minimizzazione delle proprietà. Restano da collaudare proprietà classificate e permessi nel percorso di rete, installazione reale con bundle pubblicato e release lock coordinato.

Prima di renderlo `ACTIVE`:

1. installare lo stesso nuovo secret esadecimale di 32 byte, distinto dalle altre chiavi, in APISIX (`OUF_UDP_SEARCH_OWNER_KEY`, ereditato dai worker tramite `nginx_config.envs`) e nel solo UDP come file leggibile dal processo (`OUF_UDP_SEARCH_OWNER_KEY_FILE`); fissare `OUF_UDP_SEARCH_TENANT_ID`, `OUF_UDP_SEARCH_ISSUER`, `OUF_UDP_SEARCH_AUDIENCE`, `OUF_UDP_SEARCH_WORKLOAD` ai valori effettivi del bundle/Keycloak;
2. compilare il catalogo e proiettarvi l'installazione come per il profilo incidenti; generare il candidato con `python3 -m tools.materialize_object_search --runtime <runtime.json> --oidc-client-secret-ref '$ENV://<OIDC_SECRET_ENV>' --delegation-key-env <DELEGATION_KEY_ENV> --owner-key-env <AUTHORIZATION_OWNER_KEY_ENV> --udp-key-env OUF_UDP_SEARCH_OWNER_KEY --output <candidate.json>`; verificare che i tre parametri di chiave siano distinti;
3. applicare solo la nuova rotta con `sudo python3 -m ops.apisix.deploy_object_search --materialization <candidate.json> --admin-key <admin-key-file> --backup-dir /opt/ouf/backup`: la directory privata e persistente deve esistere prima del deploy. Lo script stampa `BACKUP=<previous.json>` e verifica readback. Conservare quel percorso esatto; in caso di rollback usare `sudo python3 -m ops.apisix.deploy_object_search --restore <previous.json> --admin-key <admin-key-file> --backup-dir /opt/ouf/backup`;
4. eseguire su stack di destinazione i test positivi e negativi APISIX→UDP per token e scope mancanti, tenant errato, receipt falsificato, body alterato, tipo wildcard, `pageSize` fuori limite, assenza del bundle, proprietà minimizzate, `nextCursor` e `partial`. La CI copre una parte del percorso con bundle sintetico, come precisato sopra. Bloccare l'attivazione MCP finché collaudo, binding effettivi e release coordinata non sono registrati.

Il percorso THS per configurazioni e decisioni umane resta nel browser Onboarding; i log protetti non entrano nel manifest MCP. R-INSTALL resta OPEN.

## Preflight laboratorio del 23 settembre 2026

Verifiche read-only tramite `oufadmin` (Docker richiede `sudo`): APISIX e UDP sono sulla rete `ouf-backend`; UDP pubblica l'alias `ouf-udp-object-resolution`, usato dalla rotta candidata. Checkout puliti: Gateway `dbdc24b5481dc9473b21b360ab1142c9aef0194b`, UDP `862a975e28dc681faa6482dba1feef0acd2a3988`. Le PR candidate #51 Gateway e #31 UDP sono draft e **non installate**. Container correnti: `ouf-apisix` immagine `84e6b5e787e9`, `ouf-udp` immagine `ouf-udp:862a975e`; avvio manuale (`restart=unless-stopped`), nessun Compose rinvenuto sotto `/opt/ouf` e in `/opt/ouf/ops` non è presente uno script di deploy UDP. L'ispezione dei **soli nomi** delle variabili conferma che `OUF_UDP_SEARCH_OWNER_KEY` manca in APISIX e i cinque `OUF_UDP_SEARCH_*` richiesti mancano in UDP. Il servizio MCP attuale è `HEALTHY`, ma non prova R4a.

Al preflight la discovery OIDC di `https://auth.ouf-lab.it/realms/ouf` non pubblicizzava `urban.object.search`. La ricerca esatta nella console Keycloak ha confermato che lo scope non esisteva. Il client Inspector è solo un test. Non aggiungere lo scope al workload per deduzione: la rotta lo richiede nella delega umana e verifica separatamente l'identità del workload.

**Acceptance IAM, 23 settembre 2026: PASS.** Lo scope OpenID Connect è stato creato nel realm `ouf` con `Include in token scope=On` e associato come **Default** a `ouf-chatgpt`. La discovery dopo il binding include `urban.object.search` in `scopes_supported`; un nuovo access token generato da `Clients → ouf-chatgpt → Client scopes → Evaluate` per `giovanni-chatgpt` contiene `urban.object.search` nel claim `scope`. Sono stati riportati solo gli scope, mai il token. Il solo gate IAM è passato: deploy e collaudo APISIX→UDP restano aperti. La configurazione desiderata è versionata in Semantic Registry con `keycloak_scopes.py` e `keycloak-r4a-lab.json`.

**Binding runtime verificati read-only:** `ouf-udp` opera su `ouf-backend` come UID:GID `10004:10004`, senza mount o porte pubblicate, con `restart=unless-stopped`; nel suo environment sono presenti i riferimenti DB, S3 e THS, ma non i cinque binding SEARCH. `ouf-apisix` è su `ouf-backend` e `ouf-gateway-control` come utente `apisix`, senza porte pubblicate; monta in sola lettura `/opt/ouf/secrets/apisix-config-permissions.yaml`. I worker Nginx ereditano OIDC, delega, authorization owner e le due chiavi summary attuali; `OUF_UDP_SEARCH_OWNER_KEY` va aggiunta mantenendo tutte le voci esistenti. La projection ACTIVE è stata letta per i **soli** campi pubblici: issuer `https://auth.ouf-lab.it/realms/ouf`, audience `ouf-api-gateway`, workload `ouf-mcp-server`. Nessun valore segreto o inspect completo è stato copiato nel runbook. `OUF_UDP_SEARCH_TENANT_ID` va confrontato con il claim `tenant_id` del nuovo token HUMAN e con il bundle owner, non dedotto da `installationId`.

**Tenant e policy, 23 settembre 2026:** il token HUMAN valutato per `giovanni-chatgpt` contiene `tenant_id=ouf-lab` (solo il valore del claim è stato condiviso). La lettura amministrativa `authorization.permissions.read` per il subject `177fd705-b57f-4f9e-a23c-af9d1f3f1f75` restituisce policy `ouf-lab-authorization:14` e nessun grant configurato per `urban.object.search`; l'endpoint dichiara `CONFIGURED_GRANTS_NOT_EFFECTIVE_PERMISSIONS`. Per il test positivo preparare un grant mirato e temporaneo, sottoporlo alla conferma THS, poi verificare la decisione owner effettiva; il grant non è stato proposto o pubblicato in questa fase. Non pubblicare un grant per bypassare i gate di installazione.

**Immagine candidata, 23 settembre 2026:** dal commit UDP `3980fe2bf03ea590914e404cd58b19822a553b9c`, con `git archive` in un tar di contesto Docker senza modificare il checkout, è stata costruita `ouf-udp:r4a-3980fe2`, immagine locale `sha256:96a0327f22ab07c34eaa3255b644b55e27d955519eefb4b218d83f3143f933be`, `USER=10004:10004`. Build PASS; nessun container è stato sostituito. Il vecchio `ouf-udp:862a975e` e lo snapshot root-only restano la base di rollback, da verificare con il futuro script di rollout.

**Esito: collaudo sul server non avviato.** Prima di modificare i container occorrono una definizione dichiarativa dell'avvio UDP attuale con rollback verificabile, immagine candidata riferita al commit UDP della PR, chiave dedicata condivisa tramite env APISIX e file UDP, eredità Nginx dei worker, bundle/policy e scope effettivi, runtime projection e candidato Gateway riferiti al branch R4a. Solo dopo il preflight dei binding installare la rotta con snapshot persistente, collaudare i casi positivi/negativi e registrare l'esito. Tenere `urban.object.search` `INACTIVE` e R-INSTALL `OPEN` fino ad allora.

Prima della sostituzione dei container, eseguire dal checkout Gateway R4a `sudo python3 -m ops.apisix.snapshot_r4a_runtime`. Il comando richiede `/opt/ouf/backup` root-owned e privato (0700), acquisisce l'inspect completo di `ouf-udp` e `ouf-apisix` in un file 0600 e stampa solo il percorso. **Il file contiene i secret presenti nell'ambiente Docker: non aprirlo in chat, non caricarlo su Git, non farne una copia non cifrata fuori dal server.** È una fonte di recupero dell'avvio esistente, non uno script di restore; completare una definizione dichiarativa verificata e una prova di rollback prima di intervenire sui container.

**Preparazione reversibile, senza riavvii:** l'inspect è stato salvato in `/opt/ouf/backup/r4a-runtime-lqgphlwp/containers.inspect.json` (0600, root-only); UDP candidate `ouf-udp:r4a-3980fe2` è già stata costruita. Su server, dal commit Gateway R4a fissato, eseguire `sudo python3 -m ops.apisix.prepare_r4a_runtime --snapshot /opt/ouf/backup/r4a-runtime-lqgphlwp/containers.inspect.json --tenant ouf-lab`. Lo script richiede i due container originali ancora in esecuzione e invariati rispetto allo snapshot, la configurazione APISIX montata in sola lettura e UDP come `10004:10004`. Crea in una nuova directory 0700 accanto allo snapshot `apisix.env`, `udp.env`, un file chiave UDP 0400 posseduto da `10004:10004`, e `apisix-config.yaml` con la sola nuova voce `OUF_UDP_SEARCH_OWNER_KEY` in `nginx_config.envs`. I file env conservano le variabili Docker preesistenti e contengono segreti: **non stamparli, non caricarli in chat né versionarli**. Stampa esclusivamente il percorso `PRIVATE_R4A_CANDIDATE`; la directory non è un deploy. Verificare modalità e presenza dei quattro file tramite `sudo find <PRIVATE_R4A_CANDIDATE> -maxdepth 1 -type f -printf '%f %m %u:%g\n'` (solo metadati). Prima della sostituzione preparare e collaudare comandi di rollout e rollback che preservino memoria UDP, reti, alias, mount, utente, policy di restart e ambiente APISIX senza mostrare i secret.

**Esecuzione sul lab:** commit Gateway `5bf289f9f0cce651a6daa6f232417cdb6eeb6fa5`, CI PASS. `prepare_r4a_runtime` ha restituito `PRIVATE_R4A_CANDIDATE=/opt/ouf/backup/r4a-runtime-lqgphlwp/candidate-xja04twe` e `CANDIDATE_PREPARED; RUNNING_CONTAINERS_AND_MOUNTED_CONFIG_UNCHANGED`. Restano la verifica dei metadati dei quattro file, la procedura provata di rollout/rollback e il test runtime; nessun container risulta modificato dalla preparazione.

Metadati osservati: directory `700 root:root`; `udp-search-owner.key` `400 10004:10004`; `apisix.env` e `udp.env` `600 root:root`; `apisix-config.yaml` `400 636:636`. La proprietà del candidato YAML corrisponde al file montato attuale. Il passo seguente è `sudo python3 -m ops.apisix.preflight_r4a_runtime --snapshot /opt/ouf/backup/r4a-runtime-lqgphlwp/containers.inspect.json --candidate /opt/ouf/backup/r4a-runtime-lqgphlwp/candidate-xja04twe`. Lo script read-only confronta tutti i valori env preesistenti senza esporli, la nuova chiave, il YAML e gli ID dei container; produce solo metadati Docker utili per disegnare il rollout e richiede un'ispezione aggiuntiva se trova privilegi o network mode inattesi.
