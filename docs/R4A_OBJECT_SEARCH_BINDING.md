# R4a — urban.object.search, prima tranche

Il contratto candidate collega `POST /internal/capabilities/v1/execute/urban.object.search` a `POST /api/udp/v1/objects/search` sul servizio UDP. Il payload è `{ "type": "<tipo canonico esatto>", "pageSize": 1..100, "cursor": "<opzionale>" }`. UDP riusa il serving con tenant, decisioni per oggetto/proprietà, proiezione minimizzata e cursore opaco. Nessun parametro seleziona un backend o una query arbitraria.

**Stato: candidato installabile, tool MCP ancora `INACTIVE`.** `tools.materialize_object_search` aggiunge una sola rotta POST al profilo di incidenti esistente. Riutilizza OIDC workload, delega umana firmata e scope `urban.object.search`; verifica envelope e argomenti, cancella le credenziali in ingresso e invia a UDP solo `{type,pageSize?,cursor?}` insieme a un receipt HMAC di 30 secondi legato ai byte esatti. UDP verifica receipt, tenant e scope, installa il `TrustedPrincipal` e richiede la policy locale prima del serving. La configurazione mancante risponde 503; firma, body o policy non validi rispondono 403.

La CI Gateway esegue `OUF_APISIX_LIVE_TEST=1 python3 -m pytest -q tests/test_execute_apisix_live.py` con APISIX Docker reale, issuer OIDC e owner HTTP di test. Controlla route `/internal/capabilities/v1/execute/urban.object.search`, delega e scope, corpo inoltrato, credenziali rimosse e firma/body hash del receipt; copre anche tenant errato e argomenti vietati. Il job `object-search-pairwise` genera inoltre il receipt con il Lua Gateway e lo fa consumare, con il corpo originale e alterato, dal filtro Java UDP al commit fissato nel workflow; verificati principal, correlation e diniego della policy owner. Il job `object-search-network` usa gli stessi Gateway/APISIX e il servizio UDP Java reale con PostgreSQL e bundle di test: verifica ricerca ammessa, due pagine con cursore opaco, esclusione per tenant/oggetto e `partial` dopo il rifiuto owner. La CI UDP verifica separatamente la minimizzazione delle proprietà. Restano da collaudare proprietà classificate e permessi nel percorso di rete, installazione reale con bundle pubblicato e release lock coordinato.

Prima di renderlo `ACTIVE`:

1. installare lo stesso nuovo secret esadecimale di 32 byte, distinto dalle altre chiavi, in APISIX (`OUF_UDP_SEARCH_OWNER_KEY`, ereditato dai worker tramite `nginx_config.envs`) e nel solo UDP come file leggibile dal processo (`OUF_UDP_SEARCH_OWNER_KEY_FILE`); fissare `OUF_UDP_SEARCH_TENANT_ID`, `OUF_UDP_SEARCH_ISSUER`, `OUF_UDP_SEARCH_AUDIENCE`, `OUF_UDP_SEARCH_WORKLOAD` ai valori effettivi del bundle/Keycloak;
2. compilare il catalogo e proiettarvi l'installazione come per il profilo incidenti; generare il candidato con `python3 -m tools.materialize_object_search --runtime <runtime.json> --oidc-client-secret-ref '$ENV://<OIDC_SECRET_ENV>' --delegation-key-env <DELEGATION_KEY_ENV> --owner-key-env <AUTHORIZATION_OWNER_KEY_ENV> --udp-key-env OUF_UDP_SEARCH_OWNER_KEY --output <candidate.json>`; verificare che i tre parametri di chiave siano distinti;
3. applicare solo la nuova rotta con `sudo python3 -m ops.apisix.deploy_object_search --materialization <candidate.json> --admin-key <admin-key-file> --backup-dir /opt/ouf/backup`: la directory privata e persistente deve esistere prima del deploy. Lo script stampa `BACKUP=<previous.json>` e verifica readback. Conservare quel percorso esatto; in caso di rollback usare `sudo python3 -m ops.apisix.deploy_object_search --restore <previous.json> --admin-key <admin-key-file> --backup-dir /opt/ouf/backup`;
4. eseguire su stack di destinazione i test positivi e negativi APISIX→UDP per token e scope mancanti, tenant errato, receipt falsificato, body alterato, tipo wildcard, `pageSize` fuori limite, assenza del bundle, proprietà minimizzate, `nextCursor` e `partial`. La CI copre una parte del percorso con bundle sintetico, come precisato sopra. Bloccare l'attivazione MCP finché collaudo, binding effettivi e release coordinata non sono registrati.

Il percorso THS per configurazioni e decisioni umane resta nel browser Onboarding; i log protetti non entrano nel manifest MCP. R-INSTALL resta OPEN.

## Preflight laboratorio del 23 settembre 2026

Verifiche read-only tramite `oufadmin` (Docker richiede `sudo`): APISIX e UDP sono sulla rete `ouf-backend`; UDP pubblica l'alias `ouf-udp-object-resolution`, usato dalla rotta candidata. Checkout puliti: Gateway `dbdc24b5481dc9473b21b360ab1142c9aef0194b`, UDP `862a975e28dc681faa6482dba1feef0acd2a3988`. Le PR candidate #51 Gateway e #31 UDP sono draft e **non installate**. Container correnti: `ouf-apisix` immagine `84e6b5e787e9`, `ouf-udp` immagine `ouf-udp:862a975e`; avvio manuale (`restart=unless-stopped`), nessun Compose rinvenuto sotto `/opt/ouf` e in `/opt/ouf/ops` non è presente uno script di deploy UDP. L'ispezione dei **soli nomi** delle variabili conferma che `OUF_UDP_SEARCH_OWNER_KEY` manca in APISIX e i cinque `OUF_UDP_SEARCH_*` richiesti mancano in UDP. Il servizio MCP attuale è `HEALTHY`, ma non prova R4a.

La discovery OIDC di `https://auth.ouf-lab.it/realms/ouf` non pubblicizza `urban.object.search` in `scopes_supported` al preflight. Verificare con confronto esatto nella lista admin se lo scope esiste, poi associarlo al client HUMAN `ouf-chatgpt` e controllare il claim del nuovo token; lo script/profilo versionato è in Semantic Registry `scripts/installation/keycloak_scopes.py` e `keycloak-r4a-lab.json`. Il client Inspector è solo un test. Non aggiungere lo scope al workload per deduzione: la rotta lo richiede nella delega umana e verifica separatamente l'identità del workload.

**Esito: collaudo sul server non avviato.** Prima di modificare i container occorrono una definizione dichiarativa dell'avvio UDP attuale con rollback verificabile, immagine candidata riferita al commit UDP della PR, chiave dedicata condivisa tramite env APISIX e file UDP, eredità Nginx dei worker, bundle/policy e scope effettivi, runtime projection e candidato Gateway riferiti al branch R4a. Solo dopo il preflight dei binding installare la rotta con snapshot persistente, collaudare i casi positivi/negativi e registrare l'esito. Tenere `urban.object.search` `INACTIVE` e R-INSTALL `OPEN` fino ad allora.
